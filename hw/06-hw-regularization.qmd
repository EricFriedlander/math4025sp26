---
title: "Homework 6: Feature/Model Selection and Regularization"
author: "Your Name"
---

# Introduction

In this homework you will practice using regularization and model tuning to select models and features.

## Learning goals

In this assignment, you will...

-   Fit linear regression models using Ridge and LASSO regularization
-   Fit logistic regression models using $L_1$ regularization
-   Use grid-based techniques to choose tuning parameters
-   Explore your group's project dataset and prepare an EDA presentation

# Getting Started

There are two parts to this homework. The first part is focused on your group project dataset and will be completed in your project repository. The second part is focused on a separate dataset about NFL field goals and will be completed in this homework repository. You should complete both parts of the assignment, but they are independent of each other.

## Teams & Rules

Complete part 1 of this homework in your project teams. The following rules apply to all team assignments:

1.  You are all responsible for understanding the work that your team turns in.
2.  All team members must make roughly equal contributions to the homework.
3.  Any work committed to a repository must be committed and pushed by the person who wrote that work.

# Part 1: Project Dataset

::: {.callout-important}
## Where to submit Part 1

Complete Exercises 1–8 **in your project repository**, not in this homework repo. Add a new file called `01-eda.qmd` to the root of your project repo and complete the exercises there. Submit this part by committing and pushing to your project GitHub Classroom repository.
:::

The data for this section comes from the MIRION catalog, which you are using for your group project. Refer to the [project instructions](https://EricFriedlander.github.io/math4025sp26/project/project-instructions.html) and the [Data Wrangling Tips](https://EricFriedlander.github.io/math4025sp26/project/data-wrangling-tips.html) for details on the datasets and your assigned target variable.

## Cleaning and Preprocessing

## Exercise 1

::: {.callout-tip title="Question"}
Load `MIRION_meta_all_1.csv` and `MIRION_spec_1.csv` from your project's `data/` folder and join them on the `YB` column (see the Data Wrangling Tips for how to load and reshape `MIRION_spec_1.csv`). If your group has been assigned a **distance-dependent target variable** (`DIAM`, `MASS`, or `L_BOL`), you may optionally also load `MRT-dist.txt` and join it. Comment on any aspects of the combined dataset you find pertinent.
:::

## Exercise 2

::: {.callout-tip title="Question"}
Clean the data by:

1.  Dropping any columns that won't be useful as predictors. At minimum this includes the `YB` identifier column and all target variables **other than** your group's assigned target.
2.  Ensuring all remaining features have the correct type.
:::

## Exercise 3

::: {.callout-tip title="Question"}
Drop any rows which have a missing value in your **target** variable column. Why do we want to do this instead of trying to impute them?
:::

## Exercise 4

::: {.callout-tip title="Question"}
How many observations are left? Split the remaining data into a training and test set using an 80-20 split with `random_state=4025`. Note that later on, we will probably just apply cross-validation to the full dataset instead of using a separate test set, but for now use the train-test split.
:::

## Exercise 5

::: {.callout-tip title="Question"}
Generate a preprocessing pipeline using `ColumnTransformer` and `Pipeline` that can be used for both Ridge and LASSO regression. At a minimum it should include:

-   Imputing any remaining missing values.
-   Normalization of all numeric predictors... why?
:::

## Initial Model Fits

## Exercise 6

::: {.callout-tip title="Question"}
Fit a Ridge regression model and a LASSO model to the training data across a range of `alpha` values (use `np.logspace(-3, 5, 100)` or similar). For each model, collect the fitted coefficients at each `alpha` value and plot them — one line per feature — against `alpha` on the x-axis (use a log scale). Explain what you see and why. (Practice interview question.)
:::

## Tuning your model

## Exercise 7

::: {.callout-tip title="Question"}
Use `GridSearchCV` with `RepeatedKFold` to find the best `alpha` (according to RMSE) for your Ridge and LASSO models. **Tip:** We're fitting a lot of models here, which might take some time. Start with 2 folds and 1 repeat, possibly on a subset of your data, until you're sure it's running correctly. Then use 5 folds and 10 repeats to get your final estimate. Make use of [caching](https://quarto.org/docs/computations/caching.html) so you don't need to re-run cross-validation every time the document is rendered.
:::

## Final Model

## Exercise 8

::: {.callout-tip title="Question"}
Fit your best Ridge model and your best LASSO model to the full training set. Assess both models' performance on the **test** set using RMSE and $R^2$. Which is better? List all variables that LASSO includes in the final model. How does this compare to Ridge? What does this mean for the interpretability of a model fit with LASSO compared to Ridge?
:::

## Presentation Preparation

## Exercise 8.5

::: {.callout-tip title="Question"}
Prepare a **5-minute EDA presentation** for the class. Your presentation should cover:

1.  What your target variable measures physically (in plain language).
2.  The distribution of your target variable, including any transformations you applied and why.
3.  2–3 features that appear most strongly related to your target (with supporting plots).
4.  Any data quality issues you encountered (missing values, outliers, etc.) and how you handled or plan to handle them.
5.  One open question your group has going into the modeling phase.

You may present from slides (try [`revealjs` in Quarto](https://quarto.org/docs/presentations/revealjs/)), or directly from your `01-eda.qmd`. However, if you use the latter, make sure that you aren't jumping around too much. All group members should be prepared to speak.
:::

---

# Part 2: NFL Field Goals

The GitHub assignment for this portion of the project can be found [here](https://classroom.github.com/a/_-PMzqAA). You should complete this part of the assignment in a jupyter notebook in this repository and submit it by committing and pushing to GitHub.

In the second part of this homework, you will practice working with a classification problem. The dataset describes NFL field goal attempts. For those of you unfamiliar with American football, this is when the kicker tries to kick the ball through the uprights to score three points, as in
[this video](https://www.youtube.com/watch?v=U3L10PoEXvI).
This data set contains information on about 3000 NFL field goal attempts over three seasons. The column we will be interested in predicting is `Made`, which has value `1` or `0` indicating if the field goal was made or missed. If you want explanations for the other variables, you can look at the notes in the Excel file (the `.xlsx` file) by hovering over the variable names.

## Exercise 9

::: {.callout-tip title="Question"}
Open the data in Excel to familiarize yourself with the variables (you can hover over the column headers to see descriptions). Then load it into Python. You can load an `.xlsx` file directly using `pd.read_excel(..., engine='openpyxl')` or just save it as a csv and load it with `pd.read_csv()`. Split the data into training and test sets using an 80-20 split with `random_state=4025`.
:::

## Exercise 10

::: {.callout-tip title="Question"}
Clean the data by:

1.  Dropping any columns that seem like they won't be helpful to your analysis. There is at least one.
2.  Ensuring all features have the correct type.

What proportion of field goals in your **training set** were made? What does this mean in the context of determining a baseline level of accuracy that we want to beat?
:::

# Logistic Regression and Regularization

We will now extend what we've learned about $L_1$-regularization to logistic regression! In the same way that LASSO adds an $L_1$-norm penalty to the *objective* function for Ordinary Least Squares regression, we can add an $L_1$-norm penalty to the objective function for logistic regression. Whereas in OLS the objective function was the sum of squared errors, the objective function for logistic regression is the log-likelihood. Other than this, the principle works the same way: by including an $L_1$-regularization term, you induce sparsity.

In `sklearn`, [logistic regression](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LogisticRegression.html) with $L_1$ regularization is fit using using the following error term:

$$
\frac{1}{c}\left(\frac{\mathtt{l1\_ratio}}{n}\lVert\beta\rVert_2 + (1-\mathtt{l1\_ratio})\lVert\beta\rVert_1\right)
$$

This is equivalent to elastic net regularization, where `l1_ratio` controls the balance between $L_1$ and $L_2$ regularization. To get pure $L_1$ regularization, you would set `l1_ratio=1`. The `c` parameter is the inverse of the regularization strength (where `c = 1 / alpha`), so smaller `c` means more regularization.

## Exercise 11

::: {.callout-tip title="Question"}
Generate a preprocessing pipeline using `ColumnTransformer` and `Pipeline` that can be used for logistic regression with $L_1$ regularization. At a minimum it should include:

-   Dummy coding all categorical features.
-   Normalization of all numeric predictors... why?
:::

## Exercise 12

::: {.callout-tip title="Question"}
Fit logistic regression models, using $L_1$ regularization (i.e. `l1_ratio=1`), across a range of `C` values (use `np.logspace(-4, 2, 50)` or similar). For each value of `C`, collect the fitted coefficients and plot them, one line per feature, against `C` on the x-axis (use a log scale). Explain what you see and why. (Practice interview question.)
:::

## Exercise 13

::: {.callout-tip title="Question"}
Use `GridSearchCV` with `RepeatedStratifiedKFold` to find the best `C` (according to accuracy) for your logistic regression model. **Tip:** Start with 2 folds and 1 repeat until you're sure it's running correctly. Then use 5 folds and 10 repeats to get your final estimates.
:::

## Exercise 14

::: {.callout-tip title="Question"}
Fit your final logistic regression model on the full training set and assess its performance on the test set. Which variables are included in your final model (i.e., have non-zero coefficients)?
:::

## Exercise 15 (Very long question)

::: {.callout-tip title="Question"}
Use `GridSearchCV` with `RepeatedStratifiedKFold` to find the best $k$ for a `KNeighborsClassifier` on this data. Fit the final model on the full training set and assess its performance on the test set. How does it compare to the logistic regression model from Exercise 14?
:::

## Exercise 16 (Practice Interview Question)

::: {.callout-tip title="Question"}
Why doesn't it make sense to use $L_1$ regularization with KNN?
:::
