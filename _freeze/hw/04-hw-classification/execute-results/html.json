{
  "hash": "85a7da1c92a5b4144c4551b0d6bccf65",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Homework 4: Intro to Classification\"\nauthor: \"Your Name\"\n---\n\n\n# Introduction\n\nIn this homework we will focus on **classification**. That is when the response variable is categorical. We'll be on predicting a **binary** response variable, which is a categorical variable with only two possible outcomes. You will also practice collaborating with teams using GitHub.\n\n## Learning goals\n\nBy the end of the homework, you will...\n\n-   Be able to work create and merge branches in GitHub\n-   Fit and interpret logistic regression models\n-   Fit and evaluation binary classification models\n\n# Getting Started\n\nIn last weeks homework, you learned how to share your work using GitHub and to resolve merge conflicts. This week you'll learn about how to create and merge branches.\n\n## Clone the repo, create your own branch, & merging\n\nThe following directions will guide you through the process of setting up your homework to work as a group.\n\n\n[Click this link](https://classroom.github.com/a/PiA6tVgM) to accept the assignment and clone it to your computer.\n\n## Teamwork Portion\n\n::: {.callout-tip title=\"Question\"}\nYou will be placed in groups of 3-to-4 students. Please complete the following teamwork portion of the homework in class. Complete the following exercises on the board. Make sure you get approval from Dr. Friedlander before moving on to the rest of the homework.\n\nUse [this app](https://efriedlander.shinyapps.io/ClassificationMetrics/) to answer the following questions:\n\n1.   Look at the three possible boundaries: Plane, Circle, Parabola. Do you think logistic regression or KNN will yield a better classifier and how can you decide which is better? Explain\n2.   Test your hypothesis for each boundary.\n3.   Describe a binary classification scenario where the classes are imbalanced (e.g. Proportion Positive = 90%).\n4.   How will imbalanced classes (e.g. Proportion Positive = 90%) impact the classification metrics you identified in the previous question? \n5.   Describe a binary classification scenario where the penalty for a false positive is much higher than the penalty for a false negative (or vice versa). \n6.   For the scenario you described above, which metrics would be come more important? Change the classification threshold in Fit Model tab to try and improve the metric(s) you identified. \n:::\n\n\n# Our Data\n\nIn this homework, we will practice applying logistic regression by working with the data set `haberman.data` (you can read this just like a csv). The dataset contains cases from a study that was conducted between 1958 and 1970 at the University of Chicago's Billings Hospital on the survival of patients who had undergone surgery for breast cancer. More information about the data set, including information about the variables, is included in the file `haberman.names` (feel free to open it in a text editor to read it). We'll be trying to predict whether a patient survived after undergoing surgery for breast cancer.\n\n## Exercise 1\n\n::: {.callout-tip title=\"Question\"}\n-   Load the data set into Python using `pd.read_csv`. The `haberman.data` file does not contain column names so you will need to provide them as a list of strings to the `names=` argument. Choose sensible names based on the information in `haberman.names`. You can load it using something like `pd.read_csv(\"haberman.data\", names=...)`.\n-   In `sklearn` you do not need to convert the Survival Status variable into a Categorical variable (or object type string). However, you should be aware of what the values in the response variable represent. Make a note of what the values in the response variable represent.\n-   Give a brief summary of the data set using `.describe()` and `.info()`, noting any information you feel would be important.\n-   Split the data into a training and test set. Use a 60-40 split with `train_test_split`. Once again, please set your `random_state` to 427.\n:::\n\n# Fitting Our First Logistic Regression model\n\n## Exercise 2\n\n::: {.callout-tip title=\"Question\"}\nUsing the data from your training set build a `LogisticRegression` model from `sklearn.linear_model` to predict whether or not a patient will survive based **only on the number of axillary nodes detected**. Fit the model to your training data. Look at the `.coef_` and `.intercept_` attributes of your fitted model. Does the probability of survival increase or decrease with the number of nodes detected? \n\n:::\n\n## Exercise 3\n\n::: {.callout-tip title=\"Question\"}\nCreate a line plot number of nodes, going from 0 to 50 on the x-axis, and the predicted probability of survival on the y-axis. Based on this image, estimate the input that would be needed to give an output probability of 0.75. What does this mean in the context of the model?\n\nHint: Create a DataFrame containin a column with the same name as the column in your training set of the number of axillary nodes detected. Make sure that column contains the integers from 0 to 50. Use the `.predict_proba()` method on your fitted model to evaluate probabilities on this DataFrame. Extract the probabilities for the survival class. You can then plot this.\n:::\n\n# Classification Using a Logistic Regression Model\n\nFor a classification problem, we want a prediction of which class the outcome variable belongs to. Notice that the outputs of your logistic regression model using `predict_proba` are *probabilities*. We need to translate these into classifications. In order to get a prediction from a binomial logistic regression model, we define a **threshold**. If the output of the model is above the threshold, then we predict class 1, and if it is below the threshold we predict class 0.\n\n## Exercise 4\n\n::: {.callout-tip title=\"Question\"}\n-   For the rest of the homework, treat the patient dying as our \"Positive\" class.\n-   Using a threshold value of 0.5, obtain a vector of class predictions for the test data set (this should be the default in `predict`). You need not display it.\n-   Construct a confusion matrix using `sklearn.metrics.confusion_matrix`. Print the matrix.\n-   Using the numbers from your confusion matrix (i.e. without using evaluation metric functions from sklearn) manually compute the following:\n    -   Accuracy\n    -   Precision\n    -   Recall\n    -   Specificity\n    -   Negative Predictive Value\n:::\n\n## Baseline Model\n\nNow, we may be asking ourselves, \"Is this a good accuracy?\" and the answer is, as always, \"It depends on your data and the goals of your analysis!\". The question below illustrates some of the nuances of using accuracy as a performance metric.\n\n## Exercise 5\n\n::: {.callout-tip title=\"Question\"}\nSuppose you decided to create a super simple baseline model and just predict that everyone survives. What would the accuracy on the training set be? Note that you should not need to use construct a confusion matrix to answer this question or fit any models.\n:::\n\nPerhaps we shouldn't be so excited about the accuracy obtained in question 4! Accuracy is a good metric but it isn't perfect and suffers in situations where our classes are unbalanced.\n\n## Exercise 6\n\n::: {.callout-tip title=\"Question\"}\nA threshold of 0.5 isn't necessarily the best choice for the threshold. Write out a `for` loop to test every threshold between 0 and 1 (increase by steps of 0.01). Calculate the accuracy, recall, and precision at each threshold. Create a single line-plot with the the threshold on the x-axis and the accuracy, recall, and precision on the y-axis.\n\nHints:\n\n- There are two ways to do this: (1) use `FixedThresholdClassifier` from `sklearn.model_selection` to evaluate the metrics at each threshold, or (2) use `predict_proba` to get the probabilities and then calculate classes manually\n- You should NOT be refitting your logistic regression model at each threshold. You should only be fitting it once and then re-evaluating the class predictions and metrics at each threshold.\n:::\n\n# ROC and AUC\n\nLet's move on to a different method of measuring performance called a **Receiver Operating Curve** or **ROC** curve. Note that ROC curves can only be constructed when our target variable only has two classes. Let's first think about a few quantities:\n\n-   **true-positive rate**: the proportion of 1's which are correctly classified as 1's, sometimes referred to as the **sensitivity** or **recall**.\n-   **false-positive rate**: the proportion of 0's which are incorrectly classified as 1's. One minus the false-positive rate is a quantity called the **specificity**\n\nAs we tune our threshold above, we are changing the true-positive and false-positive rates. The higher our threshold, the fewer observations get classified as positive and so the true-positive rate will decrease and the false-positive rate will decrease. As a result, we can view the true-positive rate as a function of the false-positive rate. Plotting this function results in an ROC curve.\n\nThe more the curve looks like its being sucked into the top left corner, the better your model is. In fact, we can compute the area under this ROC curve to get a performance metric called **AUC** which you can use to evaluate your model. The nice thing about ROC curves and the AUC metric is that they are insensitive to class sizes so they can be used when you have unbalanced classes.\n\n## Exercise 7\n\n::: {.callout-tip title=\"Question\"}\nProduce an ROC curve and calculate the AUC statistic on the test set. You can use`RocCurveDisplay`, `roc_curve`, and `roc_auc_score` from `sklearn.metrics`. Report your AUC. Comment on your results.\n:::\n\n# Final Challenge\n\n## Exercise 8\n\n::: {.callout-tip title=\"Question\"}\nKaggle is a website that runs a variety of data science competitions. Read about the framingham heart study and the associated data set [here](https://www.kaggle.com/datasets/dileep070/heart-disease-prediction-using-logistic-regression). Please stop reading at the section entitled \"logistic regression\" (right after the \"Demographics\" section) as this will spoil the fun of analyzing this data set. Since you have to create an account to download data off of Kaggle I've included a csv in the `data/` directory named `framingham.csv`. \n\nCreate the best model that you can. Best model gets a high five from Dr. Friedlander. Note: split your data into a train and test sets using the random state 10520 and a 60-40 split. Be prepared to justify how you determined what \"best\" meant and present on your work in class.\n:::\n\n",
    "supporting": [
      "04-hw-classification_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}